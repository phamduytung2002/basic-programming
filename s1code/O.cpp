#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    while (n--) {
        double a, b;
        cin >> a >> b;
        cout << fixed << setprecision(16) << a / b << endl;
    }

    while(n--!=0){
        
    }
}